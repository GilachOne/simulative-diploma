# Диплом Simulative: аналитика маркетплейса

Задание: https://app.simulative.ru/course/14/1263

**Сервер и Metabase развёрнуты.** Оба исследования выполнены на всех 365 днях 2023 года. Первоначальная загрузка остальных лет с 2022-01-01 идёт последовательно; её окончание нужно подтвердить по журналу `etl_days` перед сдачей.

Дашборд: https://5.23.55.6/dashboard/2 — требуется отдельный аккаунт преподавателя. Доступы передаются приватно.

**[Открыть демодашборд без регистрации](https://5.23.55.6/public/dashboard/a37ee339-a32d-49d7-a9ca-66bd03ea02a2).** Публичная ссылка показывает агрегированные учебные данные и фильтры периода. Она не предоставляет аккаунт, редактор SQL или доступ к серверу. Доступы преподавателя публиковать не нужно.

Отчёты для чтения: [ассортимент](https://5.23.55.6/reports/01_assortment.html), [клиенты](https://5.23.55.6/reports/02_clients.html).
Результат сверки истории: https://5.23.55.6/status/history.json — `complete` означает, что календарь заполнен до указанной даты и количество строк совпадает с журналом.

## Архитектура

API → сохранённый снимок дня → проверка данных → PostgreSQL → Metabase и два исследования в Python.

- `etl.py` — последовательная загрузка, повторы при сбоях, снимки и защита от повторной вставки.
- `sql/schema.sql` — таблицы и представления для дашборда.
- `run_local.py` — запуск на текущем Windows-компьютере с приватными настройками.
- `export_research.py` — агрегаты для двух исследований, экспорт только после загрузки всех 365 дней 2023 года.
- `build_reports.py`, `run_reports.py` — сборка и выполнение двух Jupyter Notebook и HTML-отчётов.
- `metabase_setup.py` — создание дашборда через API Metabase.
- `tests/` — проверки преобразований.
- `deploy/` — ежедневное расписание и службы Linux VPS.
- `BRIEF.md` — требования и наблюдения о нестандартном поведении API.

## Запуск

Нужны Python 3.12+, PostgreSQL, переменные `PGHOST`, `PGPORT`, `PGUSER`, `PGDATABASE`, `PGPASSWORD` (или pgpass). Создайте отдельную базу `marketplace_diploma`. Пароль в Git не хранится.

```bash
python -m pip install -r requirements.txt
python etl.py --start 2022-01-01
python etl.py --daily
python -m unittest discover -s tests -v
```

Параметр `--end YYYY-MM-DD` включителен. Без него конец — вчера. `--daily` загружает только вчерашний день. Часовой пояс явно задаётся `BUSINESS_TIMEZONE`, по умолчанию Europe/Moscow.

API нельзя загружать параллельно: при проверке обнаружено смешивание дат. Общая блокировка PostgreSQL сериализует запросы нескольких экземпляров. Для защиты от дублей используется журнал `etl_days`, а не предположение, что каждая комбинация полей уникальна. Полностью одинаковые строки в одном снимке сохраняются с номером вхождения: у источника нет ID заказа/строки, поэтому нельзя безусловно считать совпадение полей дублем.

Строки принятого дня и запись о его загрузке фиксируются одной транзакцией. При ошибке вставки всё откатывается. Уже записанный исходный снимок остаётся для повторного запуска. Снимки сортируются только для контрольной суммы; порядок выдачи API может изменяться.

## Исследования

После полной загрузки 2023 года:

```bash
python export_research.py
python build_reports.py
python run_reports.py
```

В `reports/` находятся два выполненных ноутбука и два HTML-документа с кодом, графиками и выводами: ассортимент и клиенты. Исходные снимки, клиентские списки, журналы и приватные конфиги находятся в `data/` и исключены из Git. Публичные результаты содержат агрегаты.

За 2023 год получено 2 040 076 строк, принято 1 999 609, исключено 40 467 (1,98%). В анализе 49 999 SKU и 864 445 клиентов. Суммы обозначены в денежных единицах источника.

- [Исследование ассортимента](reports/01_assortment.ipynb): ABC, стабильность спроса, 10 кандидатов на проверку глубины скидок, 15 позиций для проверки перед выводом.
- [Исследование клиентов](reports/02_clients.ipynb): повторные покупки, когортное удержание, сегменты и две кампании с контрольной группой.
- HTML-файлы рядом с ноутбуками можно скачать и открыть в браузере.

## Метрики

Выручка — сумма после скидок; активные клиенты и SKU — distinct ID за период. Количество товарных строк не называется числом заказов. В API нет валюты, себестоимости, ID заказа и названий товаров. Годовая выручка на клиента не выдаётся за lifetime LTV, скидка — за маржу, сценарий кампании — за измеренный эффект.

## Проверки и сервер

PostgreSQL 18, Metabase 0.63.18, Java 25. PostgreSQL и Metabase слушают localhost; Nginx предоставляет HTTPS. Сертификат обновляется автоматически. Ежедневный таймер запускает загрузку вчерашнего дня в **07:00 Europe/Moscow**. Прежнее локальное расписание отключено, чтобы не создавать параллельные запросы к API с разных компьютеров.

Проверены все 13 запросов дашборда, полнота 2023 года, сверка строк с журналом, вход преподавателя по HTTPS. 10 тестов преобразований и 2 интеграционных теста прошли: повторный запуск сохраняет кратность одинаковых строк и не вставляет их повторно; сбой записи журнала откатывает продажи. Интеграционные тесты запускаются с `DIPLOMA_DB_TESTS=1` в базе, где тестовый пользователь может создавать временную схему.

У преподавателя отдельный аккаунт Metabase и PostgreSQL-пользователь только с SELECT. SSH-аккаунт разрешает только туннель к `127.0.0.1:5432`, без командной оболочки и других портов. Root-доступ для просмотра работы не требуется.

## Что требуется для окончательной сдачи

Подтвердить завершение первоначальной истории с 2022-01-01 до вчерашнего дня, отсутствие пропусков дат и успешное выполнение службы. Два исследования и серверный дашборд готовы; доступы передаются отдельно от публичного кода.
